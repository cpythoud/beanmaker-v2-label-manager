<?xml version="1.0" encoding="UTF-8"?>
<server description="Label Manager">

    <featureManager>
        <feature>webProfile-7.0</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="8080"
                  httpsPort="-1" />

    <webApplication contextRoot="/" location="LabelManager.war"/>

</server>
