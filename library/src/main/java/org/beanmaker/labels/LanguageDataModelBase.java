// Generated by BeanMaker V2, on June 5, 2026, 2:08:03 PM EST
// Library Version #2.4-SNAPSHOT

package org.beanmaker.labels;

import org.beanmaker.v2.runtime.annotations.DoNotEdit;

import static org.beanmaker.v2.runtime.FieldEquality.areEqual;

@DoNotEdit(generator = "org.beanmaker.v2.codegen.BeanDataModelBaseSourceFile", version = "2.4-SNAPSHOT", date = "2026-06-05T19:08:03.625417472Z")
interface LanguageDataModelBase {

	long getId();

	String getName();
	String getIso();
	String getRegion();
	Boolean isDefaultLanguage();
	Boolean isActive();
	long getItemOrder();

	boolean isNameEmpty();
	boolean isIsoEmpty();
	boolean isRegionEmpty();
	boolean isDefaultLanguageEmpty();
	boolean isActiveEmpty();

	default boolean isContentIdentical(LanguageDataModel language) {
		return areEqual(getName(), language.getName()) && areEqual(getIso(), language.getIso()) && areEqual(getRegion(), language.getRegion()) && areEqual(isDefaultLanguage(), language.isDefaultLanguage()) && areEqual(isActive(), language.isActive());
	}

}
