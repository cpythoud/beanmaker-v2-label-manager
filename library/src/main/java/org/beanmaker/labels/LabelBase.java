// Generated by BeanMaker V2, on June 5, 2026, 2:08:03 PM EST
// Library Version #2.4-SNAPSHOT

package org.beanmaker.labels;

import java.sql.ResultSet;
import java.sql.SQLException;

import java.util.ArrayList;
import java.util.List;

import org.beanmaker.v2.database.sql.DbQuerySetup;
import org.beanmaker.v2.database.sql.DbTransaction;
import org.beanmaker.v2.database.sql.SqlRuntimeException;

import org.beanmaker.v2.runtime.DBUtil;
import org.beanmaker.v2.runtime.DbBeanInitializer;
import org.beanmaker.v2.runtime.DbBeanInterface;
import org.beanmaker.v2.runtime.DbBeanLanguage;
import org.beanmaker.v2.runtime.ToStringMaker;

import org.beanmaker.v2.runtime.annotations.DoNotEdit;

import org.beanmaker.v2.util.Strings;

import static org.beanmaker.labels.DbBeans.dbAccess;

@DoNotEdit(generator = "org.beanmaker.v2.codegen.BeanBaseSourceFile", version = "2.4-SNAPSHOT", date = "2026-06-05T19:08:03.654509439Z")
abstract class LabelBase implements DbBeanInterface, LabelDataModel {

	private final long id;
	private final String name;

	LabelBase(long id) {
		final String[] name = new String[1];
		DbBeanInitializer.initialize(id, LabelParameters.INSTANCE, dbAccess, rs -> {
			name[0] = DBUtil.getString(rs, 2);
		});
		this.id = id;
		this.name = name[0];
	}

	LabelBase(long id, DbTransaction transaction) {
		final String[] name = new String[1];
		if (transaction == null)
			DbBeanInitializer.initialize(id, LabelParameters.INSTANCE, dbAccess, rs -> {
			name[0] = DBUtil.getString(rs, 2);
		});
		else
			DbBeanInitializer.initialize(id, LabelParameters.INSTANCE, transaction, rs -> {
					name[0] = DBUtil.getString(rs, 2);
				});
		this.id = id;
		this.name = name[0];
	}

	LabelBase(long id, String name) {
		this.id = id;
		this.name = name;
	}

	LabelBase(ResultSet rs) {
		this(DBUtil.getBeanID(rs, 1), DBUtil.getString(rs, 2));
	}

	public Label refreshFromDataBase() {
		return new Label(id);
	}

	@Override
	public boolean equals(Object object) {
		if (object instanceof Label)
			return ((Label) object).getId() == id;

		return false;
	}

	@Override
	public int hashCode() {
		return Long.hashCode(id);
	}

	@Override
	public String toString() {
		ToStringMaker stringMaker = new ToStringMaker(this);
		stringMaker.addField("name", name);
		return stringMaker.toString();
	}

	@Override
	public long getId() {
		return id;
	}

	@Override
	public String getName() {
		return name;
	}

	@Override
	public boolean isNameEmpty() {
		return Strings.isEmpty(name);
	}

	@Override
	public String getNameForIdNamePairsAndTitles(DbBeanLanguage language) {
		return DBUtil.getHumanReadableTitle(LabelParameters.INSTANCE, id, dbAccess, LabelManager.getBasicFunctions(), language);
	}

	public static List<Label> getAll() {
		return getAll(LabelParameters.INSTANCE.getOrderByFields());
	}

	static List<Label> getAll(String orderBy) {
		return getSelection(null, orderBy, null);
	}

	static List<Label> getSelection(String whereClause) {
		return getSelection(whereClause, (DbQuerySetup) null);
	}

	static List<Label> getSelection(String whereClause, DbQuerySetup setup) {
		return getSelection(whereClause, LabelParameters.INSTANCE.getOrderByFields(), setup);
	}

	static List<Label> getSelection(String whereClause, String orderBy, DbQuerySetup setup) {
		return DBUtil.getSelection(LabelParameters.INSTANCE, whereClause, orderBy, setup, Label::getList, dbAccess);
	}

	static long getSelectionCount(String whereClause) {
		return getSelectionCount(whereClause, (DbQuerySetup) null);
	}

	static long getSelectionCount(String whereClause, DbQuerySetup setup) {
		return DBUtil.getSelectionCount(LabelParameters.INSTANCE, whereClause, setup, dbAccess);
	}

	public static long getCount() {
		return DBUtil.getFullCount(LabelParameters.INSTANCE, dbAccess);
	}

	public static List<Label> getAll(DbTransaction transaction) {
		return getAll(LabelParameters.INSTANCE.getOrderByFields(), transaction);
	}

	static List<Label> getAll(String orderBy, DbTransaction transaction) {
		return getSelection(null, orderBy, null, transaction);
	}

	static List<Label> getSelection(String whereClause, DbTransaction transaction) {
		return getSelection(whereClause, null, transaction);
	}

	static List<Label> getSelection(String whereClause, DbQuerySetup setup, DbTransaction transaction) {
		return getSelection(whereClause, LabelParameters.INSTANCE.getOrderByFields(), setup, transaction);
	}

	static List<Label> getSelection(String whereClause, String orderBy, DbQuerySetup setup, DbTransaction transaction) {
		return DBUtil.getSelection(LabelParameters.INSTANCE, whereClause, orderBy, setup, Label::getList, transaction);
	}

	static long getSelectionCount(String whereClause, DbTransaction transaction) {
		return getSelectionCount(whereClause, null, transaction);
	}

	static long getSelectionCount(String whereClause, DbQuerySetup setup, DbTransaction transaction) {
		return DBUtil.getSelectionCount(LabelParameters.INSTANCE, whereClause, setup, transaction);
	}

	public static long getCount(DbTransaction transaction) {
		return DBUtil.getFullCount(LabelParameters.INSTANCE, transaction);
	}

	public static boolean isIdOK(long id) {
		return DBUtil.isIdOK(LabelParameters.INSTANCE, id, dbAccess);
	}

	public static boolean isIdOK(long id, DbTransaction transaction) {
		return DBUtil.isIdOK(LabelParameters.INSTANCE, id, transaction);
	}

	public static List<Label> getList(ResultSet rs) {
		List<Label> list = new ArrayList<>();

		try {
			while (rs.next())
				list.add(new Label(rs));
		} catch (SQLException sqlex) {
			throw new SqlRuntimeException(sqlex);
		}

		return list;
	}

}
