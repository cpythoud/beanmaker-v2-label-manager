// Generated by BeanMaker V2, on May 18, 2026, 2:56:32 PM EST
// Library Version #2.3-SNAPSHOT

package org.beanmaker.labels;

import java.sql.ResultSet;
import java.sql.SQLException;

import java.util.ArrayList;
import java.util.List;

import org.beanmaker.v2.runtime.DBUtil;
import org.beanmaker.v2.runtime.DbBeanInitializer;
import org.beanmaker.v2.runtime.DbBeanLanguage;
import org.beanmaker.v2.runtime.DbBeanWithItemOrder;
import org.beanmaker.v2.runtime.ToStringMaker;

import org.beanmaker.v2.runtime.annotations.DoNotEdit;

import org.beanmaker.v2.util.Strings;

import org.dbbeans.sql.DBQuerySetup;
import org.dbbeans.sql.DBTransaction;
import org.dbbeans.sql.SQLRuntimeException;

import static org.beanmaker.labels.DbBeans.dbAccess;

@DoNotEdit(generator = "org.beanmaker.v2.codegen.BeanBaseSourceFile", version = "2.3-SNAPSHOT", date = "2026-05-18T19:56:32.414294037Z")
abstract class LanguageBase implements DbBeanWithItemOrder, LanguageDataModel {

	private final long id;
	private final String name;
	private final String iso;
	private final String region;
	private final Boolean defaultLanguage;
	private final Boolean active;
	private final long itemOrder;

	LanguageBase(long id) {
		this(id, null);
	}

	LanguageBase(long id, DBTransaction transaction) {
		final String[] name = new String[1];
		final String[] iso = new String[1];
		final String[] region = new String[1];
		final Boolean[] defaultLanguage = new Boolean[1];
		final Boolean[] active = new Boolean[1];
		final long[] itemOrder = new long[1];
		if (transaction == null)
			DbBeanInitializer.initialize(id, LanguageParameters.INSTANCE, dbAccess, rs -> {
			name[0] = DBUtil.getString(rs, 2);
			iso[0] = DBUtil.getString(rs, 3);
			region[0] = DBUtil.getString(rs, 4);
			defaultLanguage[0] = DBUtil.getBoolean(rs, 5);
			active[0] = DBUtil.getBoolean(rs, 6);
			itemOrder[0] = DBUtil.getItemOrder(rs, 7);
		});
		else
			DbBeanInitializer.initialize(id, LanguageParameters.INSTANCE, transaction, rs -> {
					name[0] = DBUtil.getString(rs, 2);
					iso[0] = DBUtil.getString(rs, 3);
					region[0] = DBUtil.getString(rs, 4);
					defaultLanguage[0] = DBUtil.getBoolean(rs, 5);
					active[0] = DBUtil.getBoolean(rs, 6);
					itemOrder[0] = DBUtil.getItemOrder(rs, 7);
				});
		this.id = id;
		this.name = name[0];
		this.iso = iso[0];
		this.region = region[0];
		this.defaultLanguage = defaultLanguage[0];
		this.active = active[0];
		this.itemOrder = itemOrder[0];
	}

	LanguageBase(long id, String name, String iso, String region, Boolean defaultLanguage, Boolean active, long itemOrder) {
		this.id = id;
		this.name = name;
		this.iso = iso;
		this.region = region;
		this.defaultLanguage = defaultLanguage;
		this.active = active;
		this.itemOrder = itemOrder;
	}

	LanguageBase(ResultSet rs) {
		this(DBUtil.getBeanID(rs, 1), DBUtil.getString(rs, 2), DBUtil.getString(rs, 3), DBUtil.getString(rs, 4), DBUtil.getBoolean(rs, 5), DBUtil.getBoolean(rs, 6), DBUtil.getItemOrder(rs, 7));
	}

	public Language refreshFromDataBase() {
		return new Language(id);
	}

	@Override
	public boolean equals(Object object) {
		if (object instanceof Language)
			return ((Language) object).getId() == id;

		return false;
	}

	@Override
	public int hashCode() {
		return Long.hashCode(id);
	}

	@Override
	public String toString() {
		ToStringMaker stringMaker = new ToStringMaker(this);
		stringMaker.addField("name", name);
		stringMaker.addField("iso", iso);
		stringMaker.addField("region", region);
		stringMaker.addField("defaultLanguage", defaultLanguage);
		stringMaker.addField("active", active);
		stringMaker.addField("itemOrder", itemOrder);
		return stringMaker.toString();
	}

	@Override
	public long getId() {
		return id;
	}

	@Override
	public String getName() {
		return name;
	}

	@Override
	public String getIso() {
		return iso;
	}

	@Override
	public String getRegion() {
		return region;
	}

	@Override
	public Boolean isDefaultLanguage() {
		return defaultLanguage;
	}

	@Override
	public Boolean isActive() {
		return active;
	}

	@Override
	public long getItemOrder() {
		return itemOrder;
	}

	@Override
	public boolean isFirstInItemOrder() {
		return LanguageParameters.INSTANCE.getItemOrderManager().isFirstInItemOrder(this);
	}

	@Override
	public boolean isLastInItemOrder() {
		return LanguageParameters.INSTANCE.getItemOrderManager().isLastInItemOrder(this, dbAccess);
	}

	@Override
	public boolean isNameEmpty() {
		return Strings.isEmpty(name);
	}

	@Override
	public boolean isIsoEmpty() {
		return Strings.isEmpty(iso);
	}

	@Override
	public boolean isRegionEmpty() {
		return Strings.isEmpty(region);
	}

	@Override
	public boolean isDefaultLanguageEmpty() {
		return defaultLanguage == null;
	}

	@Override
	public boolean isActiveEmpty() {
		return active == null;
	}

	@Override
	public String getNameForIdNamePairsAndTitles(DbBeanLanguage language) {
		return DBUtil.getHumanReadableTitle(LanguageParameters.INSTANCE, id, dbAccess, LabelManager.getBasicFunctions(), language);
	}

	public static List<Language> getAll() {
		return getAll(LanguageParameters.INSTANCE.getOrderByFields());
	}

	static List<Language> getAll(String orderBy) {
		return getSelection(null, orderBy, null);
	}

	static List<Language> getSelection(String whereClause) {
		return getSelection(whereClause, (DBQuerySetup) null);
	}

	static List<Language> getSelection(String whereClause, DBQuerySetup setup) {
		return getSelection(whereClause, LanguageParameters.INSTANCE.getOrderByFields(), setup);
	}

	static List<Language> getSelection(String whereClause, String orderBy, DBQuerySetup setup) {
		return DBUtil.getSelection(LanguageParameters.INSTANCE, whereClause, orderBy, setup, Language::getList, dbAccess);
	}

	static long getSelectionCount(String whereClause) {
		return getSelectionCount(whereClause, (DBQuerySetup) null);
	}

	static long getSelectionCount(String whereClause, DBQuerySetup setup) {
		return DBUtil.getSelectionCount(LanguageParameters.INSTANCE, whereClause, setup, dbAccess);
	}

	public static long getCount() {
		return DBUtil.getFullCount(LanguageParameters.INSTANCE, dbAccess);
	}

	public static List<Language> getAllActive() {
		return getAllActive(LanguageParameters.INSTANCE.getOrderByFields());
	}

	static List<Language> getAllActive(String orderBy) {
		return getSelection("active > 0", orderBy, null);
	}

	public static List<Language> getAll(DBTransaction transaction) {
		return getAll(LanguageParameters.INSTANCE.getOrderByFields(), transaction);
	}

	static List<Language> getAll(String orderBy, DBTransaction transaction) {
		return getSelection(null, orderBy, null, transaction);
	}

	static List<Language> getSelection(String whereClause, DBTransaction transaction) {
		return getSelection(whereClause, null, transaction);
	}

	static List<Language> getSelection(String whereClause, DBQuerySetup setup, DBTransaction transaction) {
		return getSelection(whereClause, LanguageParameters.INSTANCE.getOrderByFields(), setup, transaction);
	}

	static List<Language> getSelection(String whereClause, String orderBy, DBQuerySetup setup, DBTransaction transaction) {
		return DBUtil.getSelection(LanguageParameters.INSTANCE, whereClause, orderBy, setup, Language::getList, transaction);
	}

	static long getSelectionCount(String whereClause, DBTransaction transaction) {
		return getSelectionCount(whereClause, null, transaction);
	}

	static long getSelectionCount(String whereClause, DBQuerySetup setup, DBTransaction transaction) {
		return DBUtil.getSelectionCount(LanguageParameters.INSTANCE, whereClause, setup, transaction);
	}

	public static long getCount(DBTransaction transaction) {
		return DBUtil.getFullCount(LanguageParameters.INSTANCE, transaction);
	}

	public static List<Language> getAllActive(DBTransaction transaction) {
		return getAllActive(LanguageParameters.INSTANCE.getOrderByFields(), transaction);
	}

	static List<Language> getAllActive(String orderBy, DBTransaction transaction) {
		return getSelection("active > 0", orderBy, null, transaction);
	}

	public static boolean isIdOK(long id) {
		return DBUtil.isIdOK(LanguageParameters.INSTANCE, id, dbAccess);
	}

	public static boolean isIdOK(long id, DBTransaction transaction) {
		return DBUtil.isIdOK(LanguageParameters.INSTANCE, id, transaction);
	}

	public static List<Language> getList(ResultSet rs) {
		List<Language> list = new ArrayList<>();

		try {
			while (rs.next())
				list.add(new Language(rs));
		} catch (SQLException sqlex) {
			throw new SQLRuntimeException(sqlex);
		}

		return list;
	}

}
